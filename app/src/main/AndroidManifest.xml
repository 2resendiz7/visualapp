<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.example.login">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Ejerciciolectura" />
        <activity android:name=".EjercicioNumeros" />
        <activity android:name=".Detalle" />
        <activity android:name=".TerapiaVisual" />
        <activity
            android:name=".egiratoria"
            android:label="@string/testEgiratoria" />
        <activity
            android:name=".Lea"
            android:label="@string/testLea" />
        <activity
            android:name=".menuoptotipos"
            android:label="@string/MenuOptotipos" />
        <activity
            android:name=".snellen"
            android:label="@string/testsnellen" />
        <activity
            android:name=".TestIshihara"
            android:label="@string/testIshihara" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/Menuprincipal" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.example.app.CastOptionsProvider" />

    </application>

</manifest>